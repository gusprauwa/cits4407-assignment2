#!/usr/bin/env bash

# Check if input file is provided
if [ $# -ne 1 ]; then
    echo "Usage: $0 <cleaned_tsv_file>"
    exit 1
fi

input_file=$1

# Function to calculate Pearson correlation
calculate_correlation() {
}

echo "The correlation between the year of publication and the average rating is "


echo "The correlation between the complexity of a game and its average rating is "
